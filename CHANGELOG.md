# v1.8.0 — 2026-09-13

- 今回添付されたゆりちゃん・オニャンコポンの1024×512 RGBA PNGへ差し替え。4列×2行の256×256セルで、各猫F01〜F08を行順に再生。
- 原画のRGBA・姿勢・セル内位置・既存アルファを保持。黒背景の推定除去、描き直し、中間画像、各コマの整列を使わない取り込みに変更。添付PNGは再圧縮せず使用。
- 8コマの目標周期を、ゆっくり2400ms（300ms/コマ）、ふつう1200ms（150ms/コマ）、元気800ms（100ms/コマ）に設定。
- コマを表示中もウィンドウを移動する処理を追加。位置更新は33msのタイマーを要求し、画像が同じときは位置だけを更新。
- 初期配置・呼び戻しに猫別の固定接地下端を使い、2匹の足元をそろえる。保存済み位置は保持し、複数モニターの配置先に対応する作業領域を参照。
- 既定224pxの左右歩行キャッシュは2匹で6.125MiB、元画像展開後は4MiB。CPU使用率の実測値ではなく画像バッファの計算値。
- HTMLの8コマプレビューとソース検証を更新。
- EXE名、設定保存先、自動起動の値名、単一起動の識別子、ネコシステム社ブランドを継続。小太郎・チャコさんの既存サブパッケージを同梱。
- Windows実機での起動・操作とCPU使用率の測定は未実施。今回のビルド・検証結果は `BUILD-REPORT.txt` を参照。

以下は前版までの履歴です。旧版の画像形式・コマ数・圧縮結果は、v1.8.0の素材を表すものではありません。

# v1.7.0 — 2026-09-13

- 今回添付された2枚の1254×1254 JPEGを、ゆりちゃんとオニャンコポン両方へ組み込み。
- 全32コマを左上から行順に使用し、元画像のRGBとセル内位置を保持。セルの端数は透明余白で処理。
- 黒背景の透過マスクを作成し、暗い尻尾の縞が欠けにくいようマスクの小穴を補完。
- 2匹とも16コマの再生時計を使用。別行動への素材切替をなくし、クリック時はハートで反応。
- 2匹を同時に確認できるHTMLと3.6秒のMP4を同梱。
- 全32コマ、3速度、停止・非表示・ドラッグ、全表示サイズと左右、原画とEXEの画素を検証。
- EXE名・設定保存先・自動起動の値名・ネコシステム社ブランドは継続。
- Windows実機での起動・CPU使用率、ブラウザーの実操作は未確認。

# v1.6.0 — 2026-09-13

- 添付の16コマJPEGをゆりちゃんの歩行原画として固定。原画のSHA-256をビルド時に検証。
- セル内座標とRGBを保持し、背景に透過マスクを作成。再描画・個別位置補正・中間画像を廃止。
- タイマー遅延時はコマを飛ばさず、停止・再開・速度変更でも位相を保持。
- ゆりちゃんの別ポーズへの自動遷移を停止し、クリックや接近にはハートで反応。
- 元画像と取り込み後の画像を同期して比較できるHTMLを追加。
- クリック透過をレイヤーウィンドウのスタイルにも反映。
- ネコシステム社の表示を追加し、既存のEXE名・設定パス・起動キーを維持。
- 検証した画像データのEXE内への埋め込み、元画像のRGB/座標一致、その他32画像の一致を確認。

以下は取得した既存パッケージの変更履歴です。

# v1.2.1 — 2026-09-12

- 旧8枚素材で後ろ脚がほぼ同じ姿勢に残っていたため、歩行16枚を全身画像として描き直しました。
- 素材入力を各1枚の4列×4行シート（`*_walk16_green.png`）に統一。旧8枚素材・中間8枚素材は現在のビルドでは使用しません。
- 透過PNGを各猫01〜16の番号で出力し、16種類の埋め込み画像と対応することを検証しました。
- 後ろ脚の拡大一覧、番号を表示するコマ送り画面、修正前後の通常速度・1/4速度動画を追加しました。
- 全身画像の切り替え方式、16枚×左右のキャッシュ、600msの通常歩行周期を維持しています。足運びとデスクトップ移動を同期させた実機評価は未実施です。
- 右クリックメニューと通知領域にv1.2.1を表示します。

## 確認した範囲

ビルド、16枚の再生順とループ境界、3速度、描画キャッシュ、位置のみ更新、埋め込み画像、Windows x64 PE構造のテストに合格。その他の16画像はv1.2.0と画素単位で一致しています。

全16枚と後ろ脚の拡大一覧を目視し、手前・奥の後ろ脚の前後位置が入れ替わることを確認しました。生成画像の観察に基づく修正で、生体運動との一致を数値評価したものではありません。

比較動画は素材を再生したものでWindowsの録画ではありません。通常動画は80fps・240枚・3秒、低速動画は20fps・240枚・12秒で全編デコードを確認。HTMLは32枚の埋め込み画像とJavaScript構文を確認しましたが、ブラウザーでの操作試験は実行環境にブラウザーがなく未実施です。Windows実機の動作・CPU使用率も未確認です。

## ローカル画像圧縮

PNG形式・1254×1254の寸法を保ち、非可逆tiny／192色、safe-strip設定を使用。外部圧縮サービスへの送信はありません。

| 素材 | 圧縮前 | 圧縮後 | 削減量 | 削減率 | SSIM | PSNR |
|---|---:|---:|---:|---:|---:|---:|
| ゆりちゃん | 1,659,902 B | 778,297 B | 881,605 B | 53.11% | 0.992894 | 39.865 dB |
| オニャンコポン | 1,716,376 B | 754,923 B | 961,453 B | 56.02% | 0.996252 | 40.470 dB |

詳細は `source/art_source/compression-walk16-report.json` を参照してください。

# v1.2.0 — 2026-09-11

歩行画像を1匹8枚から16枚へ増やしました。前の8枚は画素単位で維持し、その間に全身の中間姿勢を8枚追加しました。最後と最初の間にも中間画像を入れています。

## 再生と負荷対策

- 通常の一周は600ms、1枚の目標間隔は75msから37.5msに変更。
- ゆっくり：1200ms/周・75ms/枚、ふつう：600ms/周・37.5ms/枚、元気：400ms/周・25ms/枚。
- 小数の間隔を毎回整数に丸めず、経過時間からコマ番号を求めて一周の時間を保ちます。
- 描画タイマーを25msから15msに変更。行動判断は50msのままです。実際の表示間隔はWindowsのタイマー精度と負荷に依存します。
- 各猫16枚×左右2方向の画像をキャッシュ。画像が同じなら位置のみ更新し、位置も同じなら描画を省略します。
- キャッシュは通常サイズ224px・2匹で12.25MiB（8枚版から6.125MiB増加）。元画像の展開メモリは2匹で12MiB（4MiB増加）です。
- 右クリックメニューと通知領域にv1.2.0を表示。

## 検証

Windows x64向けビルド、3速度の16枚再生・小数の切り替え境界・一周の境界・移動補間、3サイズ×左右のキャッシュ画素、位置だけの描画更新、ハート、一時停止、非表示のテストに通過しました。

埋め込み画像は48枚で、重複はありません。既存の32枚（歩行16枚＋その他16枚）は前のEXEと画素単位で一致しています。RLE復号と乗算済みアルファを検証しました。

描画テストはAPIスタブを使ったホスト上の検証です。Windows実機での起動・操作・CPU使用率の実測は未実施です。比較動画は素材再生で、Windows画面の録画ではありません。

## 追加素材の圧縮

ローカル圧縮、PNG形式・寸法維持、**非可逆tiny／192色**（png-quantized-192-colors）。外部圧縮サービスは使用していません。

| 素材 | 圧縮前 | 圧縮後 | 削減量 | 削減率 | SSIM | PSNR |
|---|---:|---:|---:|---:|---:|---:|
| ゆりちゃん（追加8枚の素材） | 1,588,607 B | 667,661 B | 920,946 B | 57.97% | 0.991387 | 40.151 dB |
| オニャンコポン（追加8枚の素材） | 1,684,075 B | 768,413 B | 915,662 B | 54.37% | 0.996885 | 40.689 dB |

形式・寸法・フレーム数・画質の検証に通過しました。メタデータ方針はsafe-strip。192色だけを候補にし、tinyの画質検証基準は変更していません。数値は追加した素材PNGについての実測で、EXEの削減率ではありません。

アプリ名、設定フォルダー、自動起動登録名、単一起動の識別子は維持しています。起動中の旧アプリを終了し、同じ場所のEXEを置き換えてください。同梱の小太郎・チャコさん版は従来版です。

---

# v1.1.0 — 2026-09-10

ゆりちゃんとオニャンコポンの歩行画像を1匹4枚から8枚に増やし、画像の切り替えと移動を細かくしました。

## 変更内容

- 元の姿を参考に8段階の全身歩行画像を作成し、鼻先と接地ラインを合わせました。
- 通常速度で150ms/枚から75ms/枚に変更。一周600msと通常時の移動速度は維持しています。
- サイズ・左右反転済みの歩行画像をキャッシュします。毎回のリサイズを省きます。
- 画像が同じときは位置だけ更新し、位置も同じなら描画処理を省略します。
- 動きの表示は25msを目標に更新し、行動判断と待ち時間は元の50ms刻みで保っています。
- 右クリックメニューと通知領域の説明にバージョンを表示します。

## 検証

- Windows x64用の実行ファイルをビルド済み。
- 3段階の速度で、8枚の再生順・等しい表示間隔・ループ境界を確認。
- 3サイズ×左右2方向のキャッシュ画像と参照画像が一致。
- 描画コードをAPIスタブで実行し、位置だけの更新・停止中の省略・ハート・一時停止・非表示を確認。
- 埋め込み画像32枚に重複なし。RLE復号・サイズ・透過・乗算済みアルファを検証。
- 歩行以外の16枚は、旧EXEの埋め込み画像と画素単位で一致。
- Windowsの実機での起動・操作・CPU使用率の測定は未実施。比較動画は埋め込み素材の再生であり、Windows画面の録画ではありません。

画像キャッシュの追加メモリは2匹・通常サイズ224pxで6.125MiBです。画像を増やしたことで、元画像の展開メモリも2MiB増えます。CPU使用率の増減を実測値として保証するものではありません。

## 歩行素材のローカル圧縮結果

新しいPNGスプライトシート2枚を、形式・縦横サイズを変えずに圧縮しました。
設定：**非可逆・tiny／192色**（png-quantized-192-colors）。外部圧縮サービスは使用していません。

| 素材 | 圧縮前 | 圧縮後 | 削減量 | 削減率 | SSIM | PSNR |
|---|---:|---:|---:|---:|---:|---:|
| ゆりちゃん | 1,629,561 B | 710,151 B | 919,410 B | 56.42% | 0.989773 | 39.477 dB |
| オニャンコポン | 1,640,620 B | 707,692 B | 932,928 B | 56.86% | 0.995772 | 39.930 dB |

両画像とも形式・寸法・フレーム数・画質検証を通過しました。メタデータ方針はsafe-strip（表示に必要な色・向きの情報を維持）です。192色だけを候補にし、tinyの画質検証基準は変更していません。数値は元のPNG素材についての実測であり、EXE全体の削減率ではありません。

## 更新方法と互換性

起動中のアプリを右クリックから終了し、既存の `mknkDesktopCats.exe` を同じフォルダーで置き換えてください。設定フォルダー、アプリID、単一起動の識別子、自動起動登録名は維持しています。旧版と同時に起動することはできません。

同梱の小太郎・チャコさん版は旧バージョンのまま収録しています。
